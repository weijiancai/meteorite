apply plugin: "java"

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':core')
    compile project(':fxbase')
    testCompile 'junit:junit:4.+'

    compile group: 'commons-logging', name: 'commons-logging', version: '1.1.3'
}